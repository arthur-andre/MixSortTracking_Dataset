# Assets

| Fichier | Description |
|---------|-------------|
| **`demo_compressed.gif`** | Démo du README — suivi MixSort (CHI vs NYK, boîtes + ID) |
| `demo.gif` | Version pleine résolution (local, gitignored si volumineux) |

Régénérer : `scripts/make_demo_gif.sh` ou voir `output/README.md`.
